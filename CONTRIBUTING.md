# Contributing

```bash
bun install
bun run dev
bun run typecheck
bun run build
```

Content lives in `content/site.ts`. Keep the homepage typographic: white background, headings, no animation utilities, no pills.
